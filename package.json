{
  "name": "clawforce",
  "version": "0.1.0",
  "description": "Deploy AI agents via OpenClaw and Docker Compose",
  "type": "module",
  "bin": {
    "clawforce": "./bin/clawforce.mjs"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "tsx src/cli.ts",
    "build": "tsdown src/cli.ts src/index.ts --format esm --dts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit",
    "precommit:root": "pnpm typecheck && pnpm test",
    "precommit:dashboard": "pnpm --dir clawforce-dashboard lint && pnpm --dir clawforce-dashboard test",
    "check:root": "pnpm typecheck && pnpm build && pnpm test && pnpm knip --include dependencies,unlisted,unresolved",
    "check:dashboard": "pnpm --dir clawforce-dashboard lint && pnpm --dir clawforce-dashboard build && pnpm --dir clawforce-dashboard test && pnpm knip --directory clawforce-dashboard --include dependencies,unlisted,unresolved",
    "check:security": "pnpm audit --prod && pnpm --dir clawforce-dashboard audit --prod",
    "precommit:verify": "pnpm check:root && pnpm check:dashboard && pnpm check:security",
    "prepush:verify": "pnpm precommit:verify",
    "prepare": "husky"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "chalk": "^5.6.2",
    "commander": "^14.0.3",
    "nodemailer": "^8.0.1",
    "yaml": "^2.8.2",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/nodemailer": "^7.0.10",
    "@vitest/coverage-v8": "^4.0.18",
    "husky": "^9.1.7",
    "knip": "^5.83.1",
    "tsdown": "^0.20.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
