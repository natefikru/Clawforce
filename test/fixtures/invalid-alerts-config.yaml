name: "invalid-alerts"

agents:
  - name: "test-agent"
    role: "research-agent"

openclaw:
  default:
    channels:
      discord:
        enabled: true

alerts:
  notifications:
    email:
      enabled: true
