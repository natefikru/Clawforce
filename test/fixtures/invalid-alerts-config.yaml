name: "invalid-alerts"

agents:
  - name: "test-agent"
    workspace: "./workspaces/test-agent"

openclaw:
  default:
    channels:
      discord:
        enabled: true

alerts:
  notifications:
    email:
      enabled: true
