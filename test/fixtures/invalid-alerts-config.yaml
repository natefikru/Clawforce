name: "invalid-alerts"
role: "research-agent"

openclaw:
  channels:
    discord:
      enabled: true

models:
  primary: "anthropic/claude-sonnet-4-5"
  api_key: "${ANTHROPIC_API_KEY}"

alerts:
  notifications:
    email:
      enabled: true
