name: "invalid-alerts"

agents:
  - name: "test-agent"
    role: "research-agent"

openclaw:
  default:
    channels:
      discord:
        enabled: true

models:
  cloud: "anthropic/claude-sonnet-4-5"
  provider_keys:
    anthropic: "${ANTHROPIC_API_KEY}"

alerts:
  notifications:
    email:
      enabled: true
