name: "minimal-invalid-agent-runtime"

agents:
  - name: "research-agent"
    role: "research-agent"
    runtime: "other-runtime"

openclaw:
  default:
    channels:
      discord:
        enabled: true
