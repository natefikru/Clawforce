name: "minimal-invalid-agent-runtime"

agents:
  - name: "research-agent"
    workspace: "./workspaces/research-agent"
    runtime: "other-runtime"

openclaw:
  default:
    channels:
      discord:
        enabled: true
