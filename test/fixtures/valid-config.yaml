name: "test-corp"

agents:
  - name: "test-agent"
    workspace: "./workspaces/test-agent"

openclaw:
  default:
    channels:
      discord:
        enabled: true

approval:
  mode: "hybrid"
  require_approval_for:
    - "browser"
    - "message.send"

routing:
  sensitivity:
    keywords:
      - "ssn"
      - "credit card"
    pii_detection: true
