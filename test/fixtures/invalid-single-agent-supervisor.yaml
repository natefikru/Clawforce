name: "invalid-single-supervisor"

agents:
  - name: "supervisor-agent"
    workspace: "./workspaces/supervisor-agent"
    supervises: []

openclaw:
  default:
    channels:
      discord:
        enabled: true
