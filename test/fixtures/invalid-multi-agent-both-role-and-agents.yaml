name: "invalid-both"

agents:
  - name: inbox-analyst
    role: inbox-analyst
  - name: extra-agent
    role: inbox-analyst

models:
  cloud: "anthropic/claude-sonnet-4-5"
  provider_keys:
    anthropic: "${ANTHROPIC_API_KEY}"

openclaw:
  default:
    channels:
      discord:
        enabled: true
