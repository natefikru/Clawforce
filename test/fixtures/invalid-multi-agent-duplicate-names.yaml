name: "invalid-dupes"

agents:
  - name: my-agent
    workspace: "./workspaces/my-agent"
  - name: my-agent
    workspace: "./workspaces/my-agent"

openclaw:
  default:
    channels:
      discord:
        enabled: true
