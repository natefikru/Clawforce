name: "invalid-supervisor"

defaults:
  models:
    cloud: "anthropic/claude-sonnet-4-5"
    provider_keys:
      anthropic: "${ANTHROPIC_API_KEY}"

agents:
  - name: ultron
    role: supervisor
    channels:
      - type: channel
        channels: ["#ai-ops"]
    supervises: [nonexistent-agent]

openclaw:
  channels:
    discord:
      enabled: true
