name: "runtime-e2e-cloud"

agents:
  - name: "inbox-analyst"
    workspace: "./workspaces/inbox-analyst"

openclaw:
  default:
    channels:
      discord:
        enabled: true

gateway:
  bind: "loopback"

routing: {}

compliance:
  enabled: true

dashboard:
  enabled: true
  port: 4411
  auth:
    enabled: false
