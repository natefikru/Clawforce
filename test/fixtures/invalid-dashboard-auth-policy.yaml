name: "dashboard-auth-policy-invalid"

agents:
  - name: "test-agent"
    role: "inbox-analyst"

openclaw:
  default:
    channels:
      discord:
        enabled: true

dashboard:
  enabled: true
  port: 3000
