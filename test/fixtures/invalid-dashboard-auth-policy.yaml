name: "dashboard-auth-policy-invalid"

agents:
  - name: "test-agent"
    workspace: "./workspaces/test-agent"

openclaw:
  default:
    channels:
      discord:
        enabled: true

dashboard:
  enabled: true
  port: 3000
