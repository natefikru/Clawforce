name: "dashboard-auth-policy-invalid"
role: "inbox-analyst"

openclaw:
  channels:
    discord:
      enabled: true

models:
  primary: "anthropic/claude-sonnet-4-5"
  provider_keys:
    anthropic: "${ANTHROPIC_API_KEY}"

dashboard:
  enabled: true
  port: 3000
