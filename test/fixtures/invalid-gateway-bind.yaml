name: gateway-bind-invalid
role: inbox-analyst
openclaw:
  channels:
    discord:
      enabled: true
models:
  primary: "anthropic/claude-sonnet-4-5"
  api_key: "${ANTHROPIC_API_KEY}"
gateway:
  bind: "public"
