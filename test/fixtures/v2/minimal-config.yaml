name: "minimal"

models:
  cloud: "anthropic/claude-sonnet-4-5"
  provider_keys:
    anthropic: "${ANTHROPIC_API_KEY}"

agents:
  - name: my-agent
    role: research-agent

openclaw:
  default:
    channels:
      discord:
        enabled: true
