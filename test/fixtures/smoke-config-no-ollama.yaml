name: "smoke-no-ollama"

agents:
  - name: "test-agent"
    workspace: "./workspaces/test-agent"

openclaw:
  default:
    channels:
      discord:
        enabled: true

dashboard:
  enabled: true
  port: 3310
  auth:
    enabled: false
