name: "smoke-no-ollama"
role: "inbox-analyst"

openclaw:
  channels:
    discord:
      enabled: true

models:
  primary: "anthropic/claude-sonnet-4-5"
  api_key: "${ANTHROPIC_API_KEY}"

dashboard:
  enabled: true
  port: 3310
  auth:
    enabled: false
