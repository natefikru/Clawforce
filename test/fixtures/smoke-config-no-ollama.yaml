name: "smoke-no-ollama"

agents:
  - name: "test-agent"
    role: "inbox-analyst"

openclaw:
  default:
    channels:
      discord:
        enabled: true

dashboard:
  enabled: true
  port: 3310
  auth:
    enabled: false
