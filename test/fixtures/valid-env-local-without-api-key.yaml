name: "env-local-no-api-key"

agents:
  - name: "test-agent"
    role: "inbox-analyst"

openclaw:
  default:
    channels:
      discord:
        enabled: true

models:
  cloud: "ollama/llama3.3:8b"
  credential_mode: "env"
