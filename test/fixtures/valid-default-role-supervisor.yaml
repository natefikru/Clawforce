name: "default-supervisor"

agents:
  - name: "supervisor-agent"
    role: "supervisor"
    supervises: ["worker"]
  - name: "worker"
    role: "inbox-analyst"

openclaw:
  default:
    channels:
      discord:
        enabled: true
