name: "default-supervisor"

agents:
  - name: "supervisor-agent"
    workspace: "./workspaces/supervisor-agent"
    supervises: ["worker"]
  - name: "worker"
    workspace: "./workspaces/worker"

openclaw:
  default:
    channels:
      discord:
        enabled: true
