name: "auth-profile-valid"

agents:
  - name: "test-agent"
    role: "inbox-analyst"

openclaw:
  default:
    channels:
      discord:
        enabled: true

models:
  cloud: "anthropic/claude-sonnet-4-5"
  credential_mode: "auth_profile"
  auth_profile: "corp-prod"
