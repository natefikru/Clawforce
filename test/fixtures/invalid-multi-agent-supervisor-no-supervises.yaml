name: "invalid-supervisor-no-supervises"

agents:
  - name: ultron
    workspace: "./workspaces/ultron"
    supervises: []
  - name: inbox-analyst
    workspace: "./workspaces/inbox-analyst"

openclaw:
  default:
    channels:
      discord:
        enabled: true
