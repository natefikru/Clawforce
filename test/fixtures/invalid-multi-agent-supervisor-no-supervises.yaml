name: "invalid-supervisor-no-supervises"

agents:
  - name: ultron
    role: supervisor
  - name: inbox-analyst
    role: inbox-analyst

openclaw:
  default:
    channels:
      discord:
        enabled: true
