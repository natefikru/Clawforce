name: "invalid-supervisor-no-supervises"

models:
  cloud: "anthropic/claude-sonnet-4-5"
  provider_keys:
    anthropic: "${ANTHROPIC_API_KEY}"

agents:
  - name: ultron
    role: supervisor
  - name: inbox-analyst
    role: inbox-analyst

openclaw:
  default:
    channels:
      discord:
        enabled: true
