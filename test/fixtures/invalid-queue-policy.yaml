name: queue-policy-test

agents:
  - name: "test-agent"
    workspace: "./workspaces/test-agent"

openclaw:
  default:
    channels:
      discord:
        enabled: true

routing:
  health_check:
    failover_policy: queue
